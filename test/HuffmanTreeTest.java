import org.junit.Test;
import org.junit.Assert.*;

public class HuffmanTreeTest {

    @Test
    {
        leaves.forEach(nodeData);
        System.out.println("First Poll: " + ((char)leaves.poll().charCode) + " " + ((char)leaves.poll().charCode));
        System.out.println("");
        leaves.forEach(nodeData);
        System.out.println("Second Poll: " + ((char)leaves.poll().charCode) + " " + ((char)leaves.poll().charCode));
        System.out.println("");
        leaves.forEach(nodeData);
        System.out.println("third Poll: " + ((char)leaves.poll().charCode) + " " + ((char)leaves.poll().charCode));
        System.out.println("");
        leaves.forEach(nodeData);
        System.out.println("Fourth Poll: " + ((char)leaves.poll().charCode) + " " + ((char)leaves.poll().charCode));
        System.out.println("");
    }
}
